# まちまもMAP 開発ロードマップ

更新：2026-09-12。既存の公開 main を基準とした追加開発計画。

## 実装済み

- Leaflet地図、LINE認証、Supabase、既存RLS/RPC、ポイント、管理者ダッシュボード。
- 投稿と安全スポットの種類別表示ON/OFF、初期全表示、端末への選択保存、初回チュートリアル、マイページの表示ガイド。
- 安全スポットの詳細に「📍ここまでナビ」。既存の緯度・経度をGoogle Maps URLへ渡す。originを省略しGoogleマップ側で現在地を利用する。現在地が使えない場合は出発地入力／経路プレビューとなる。
- 対象：交番、駐在所、警察署、AED。子供110番も同じポップアップを利用できる構成。子供110番の公開データ追加は未実施。
- ナビ用Google Maps APIキー、独自ルート計算、地図の置き換えは不要。スマートフォンのアプリ起動可否はOS・ブラウザのリンク設定に依存する。
- 添付問題データを車120問・自転車110問、計230問へ統合。既存の重複除外・10問出題・採点・ポイント処理を維持。問題の全件法令監修は別途必要。

## 安全スポットの全国展開（継続）

- 警察施設：全国データ13,116件を登録済み。全国の全施設を網羅したという意味ではない。
- AED：東京23区すべて、計5,743件を本番登録済み。自治体の公式公開データを使用し、出典・ライセンス・基準日を各レコードに保持している。
- 次は神奈川 → 千葉 → 埼玉 → 山梨 → その他全国の順で、再利用可能な自治体公式データを追加する。
- 出典・ライセンス・基準日・座標の取得元を保持。取得失敗、座標なし、重複、区外施設、外部利用不可を区別する。準備済み件数と本番反映件数を混同しない。
- 東京23区対応は完了。世田谷区は「せたがやiMap」の公式APIから357件を登録した。
- AEDMは再利用条件が確認できるまで収集元に採用しない。

## 次の段階：近くの安全スポット

1. 現在地の許可を受け、直線距離と近い順の一覧を表示する。位置情報拒否・取得失敗時の案内も用意する。
2. 「最寄りのAED」「最寄りの交番・警察署」「最寄りの子供110番」を用意。AEDは地図上の専用ボタン → 最寄り候補 → ナビの少ない操作を目指す。
3. 地図の表示範囲・取得上限に依存しない近傍検索RPCを設計する。現在の取得上限1,500件と警察優先順では最寄りAEDを保証できないため、画面内マーカーだけで最寄りと断定しない。
4. 直線距離は経路距離・到着時間と区別する。AEDの公開範囲、入口、利用時間の情報も整備する。
5. 子供110番は掲載承諾と公開範囲を確認してからデータ・表示フィルターへ追加する。

## その後の拡張

- Googleマップ／Appleマップの選択。buildSafetyNavigationUrlのprovider境界へApple用URL生成を追加し、表示UIと接続する。
- 徒歩・自転車・車別の所要時間：当面は遷移先で表示。アプリ内表示はルートAPIの料金・利用条件・対応地域・精度を確認して別途導入する。直線距離から正確な所要時間を推定しない。

## 変更・検証方針

- 同時開発中のmainを更新直前にも確認し、競合時は最新版へ差分を適用する。
- 既存の認証・権限・ポイント・採点・管理画面をナビ追加に伴って変更しない。
- 座標検証、URL生成、ポップアップ、出題件数・回答インデックス・標識描画・重複除外を確認する。
- Google Maps URL仕様：https://developers.google.com/maps/documentation/urls/get-started

## 2026-09-11 作業記録

- ナビ・230問の変更は f50b66e、ロードマップは 630bf15 で保存。両方のVercelステータスはsuccess。
- JavaScript構文、添付との230問完全一致、200回の抽選、座標の異常値、5種類の詳細ポップアップをコード上で検証済み。
- 公開画面の実タップ確認はブラウザ接続不調で未完了。iPhone実機のGoogle Mapsアプリ起動確認は別途必要。
- 外部配信ファイル・追加自治体データの取得は接続制約で中断。本番AED追加はこの変更では行っておらず1,191件。

## 2026-09-12 作業記録

- 本番DBを再集計し、世田谷区を除く22区・5,386件が登録済みであることを確認した。
- 世田谷区公式「せたがやiMap」のAEDレイヤーを取得する再現可能なインポーターを追加した。通常レイヤー357件と24時間利用可レイヤー22件を施設名・住所で統合し、重複を除いた357件を登録した。
- 世田谷区データの基準日は公式地図の表示に基づく2025年8月1日。出典ページ、ライセンス、座標取得元を各レコードに保持した。
- 東京23区のAEDは合計5,743件となり、23区対応を完了した。
