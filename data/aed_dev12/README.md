# 開発12 再開記録

保存済み1,726件の位置照合結果を外部通信なしで再集計。既存座標と一致する町域等の代表点306件を追加で公開保留。DB削除なし。ロールバック検証・本番commit・再集計を確認。公開AEDは43,598件、掲載497/1,741自治体、未掲載1,244自治体。大網白里市・大東市は掲載0件に戻ったため復旧対象。

位置精度8かつ既存座標一致596件、低精度座標一致688件（開発11の382件を含む）、座標差異等434件、照合エラー8件。照合の成功は施設の現地稼働確認ではない。

`quarantine_low_precision.sql` は実行済み。再実行しない。`quarantine_previous_state.json` は変更前の公開状態。`confirmed_low_precision.json` は照合根拠。未加工の入力住所・照合ログは公開リポジトリに追加していない。

次は開発11全国台帳の候補あり66自治体の未処理情報源を一括処理。情報源探索が必要な1,176自治体も未完了。全施設網羅は未完了。

開発11に記録された既存住所の外部照合に対する自動承認拒否は未解除。このターンは外部照合を再試行せず、既存ローカル結果だけを使用した。
